spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/xiaomi?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: zdh239715

server:
  port: 8089

mybatis:
  # 指定 mapper.xml 的位置
  mapper-locations: classpath:mapper/*.xml


